#' read_data
#'
#' create a list of matrices for multiple factor analysis by sets
#' @param data: data set (matrix or data frame).
#' @param sets: list of vectors indicating the sets of variables (i.e. the blocks).
#' @export
#'

data = read.csv("wines.csv")
read.data = function(data, sets){

}

